the following are the apps(in rest-service folder):
. cardetails
. carsinlocation
. carstatus
. completetrip
. scheduletrip
. fareestimates


to serialize a datatype to a json just do json.dumps(myobj,cls=DataTypeJSONEncoder)

the following are the datatypes we will be using:
.in each datatype include a function toJSON for serializing
.car:
    car,cardriver,carstatus
.location:
    geolocation



TODO: finish writing all the datatypes with unit tests
    . finish all the models/tables we will be needing 

To run test cases use the command:
python manage.py test path.to.testclass